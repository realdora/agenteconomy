.next/
node_modules/
output/
test-results/
tsconfig.tsbuildinfo
.DS_Store
**/.DS_Store

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.vercel
.env*.local
# Public repo: block every env file shape, not just *.local. The pipeline keys
# (Dune, Cloudflare Radar, OpenRouter) live in env files locally, and a plain
# `.env` is the conventional name a tool or a person reaches for first.
.env
.env.*
!.env.example
